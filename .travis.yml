language: android

android:
  components:
    - tools
    - android-23
    - build-tools-23.0.3
    - platform-tools
    - extra

jdk:
  - oraclejdk8

script:
   - ./gradlew build test jacocoTestReport generateRelease

after_success:
   - bash <(curl -s https://codecov.io/bash)

branches:
  only:
  - master
  - dev
